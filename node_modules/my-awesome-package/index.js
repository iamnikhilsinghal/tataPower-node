var util = require('util');

module.exports.hello = function hello (name) {
  return util.format('Hi %s!', name);
};

module.exports.goodbye = function goodbye (name) {
  return util.format('Goodbye %s!', name);
};
